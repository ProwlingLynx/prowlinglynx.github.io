export const Footer = () => {
  return (
    <footer className={`flex-row`}>
      <nav>
        <button>Contact Me!</button>
        <button>Back to the top</button>
      </nav>
    </footer>
  );
};
